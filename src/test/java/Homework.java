import org.junit.jupiter.api.Test;

public class Homework extends BaseTest {

    //Add client via API and delete it via UI. Add also checks to make sure operations worked as expected
    //Create test for client search which does not return result
    //Create test for client search by email (add new method in ClientPage)

   
}
